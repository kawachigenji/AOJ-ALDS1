#include <bits/stdc++.h>
#define PI 3.14159265359
#define NIL (-1)
using namespace std;
const int64_t MOD = 1e9 + 7;

int factorial(int N) {
	if (N == 1) return 1;
	else return N * factorial(N - 1);
}

int main() {
	int N;
	N = 5;

	int ans = factorial(N);

	cout << ans << endl;
}
